{
  "name": "pasarela-pagos",
  "version": "1.0.0",
  "description": "Aplicación web de pasarela de pagos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run install-client && npm run build"
  },
  "keywords": ["pagos", "stripe", "ecommerce", "node", "react"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "stripe": "^12.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
